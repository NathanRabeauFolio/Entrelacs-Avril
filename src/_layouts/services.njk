---
layout: base.njk
section: page
---
	<body>
		{% include "partials/site-header.njk" %}
		{% include "partials/drawer.njk" %}

<div class="page-container services">
  <h1>{{ title }}</h1>
  {% if subtitle %}
    <p>{{ subtitle }}</p>
  {% endif %}
  {{ layoutContent | safe | replace("<p><picture>", "<picture>")
						              | replace("</p></picture>", "</picture>") }}
</div>
